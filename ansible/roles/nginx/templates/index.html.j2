<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>¡Servidor Web Configurado con Ansible!</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            max-width: 600px;
            width: 90%;
        }
        h1 {
            color: #333;
            margin-bottom: 1rem;
        }
        .success {
            color: #28a745;
            font-size: 1.2em;
            margin: 1rem 0;
        }
        .info {
            background: #f8f9fa;
            padding: 1rem;
            border-radius: 5px;
            margin: 1rem 0;
            border-left: 4px solid #007bff;
        }
        .server-info {
            text-align: left;
            margin: 1rem 0;
        }
        .badge {
            background: #007bff;
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 3px;
            font-size: 0.8em;
        }
        footer {
            margin-top: 2rem;
            font-size: 0.9em;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🎉 ¡Servidor Configurado Exitosamente!</h1>
        
        <p class="success">✅ Tu servidor Nginx está funcionando perfectamente</p>
        
        <div class="info">
            <h3>Información del Servidor</h3>
            <div class="server-info">
                <p><strong>Hostname:</strong> {{ ansible_hostname }}</p>
                <p><strong>Sistema Operativo:</strong> {{ ansible_distribution }} {{ ansible_distribution_version }}</p>
                <p><strong>Arquitectura:</strong> {{ ansible_architecture }}</p>
                <p><strong>IP del Servidor:</strong> {{ ansible_default_ipv4.address }}</p>
                <p><strong>Memoria Total:</strong> {{ (ansible_memtotal_mb / 1024) | round(1) }} GB</p>
                <p><strong>CPUs:</strong> {{ ansible_processor_vcpus }}</p>
                <p><strong>Versión de Ansible:</strong> <span class="badge">{{ ansible_version.full }}</span></p>
            </div>
        </div>

        <div class="info">
            <h3>🔧 Configurado con Ansible</h3>
            <p>Este servidor ha sido configurado automáticamente usando:</p>
            <ul style="text-align: left; display: inline-block;">
                <li>Rol <code>common</code> - Configuración base y seguridad</li>
                <li>Rol <code>nginx</code> - Servidor web Nginx</li>
                <li>Firewall UFW configurado</li>
                <li>Actualizaciones automáticas de seguridad</li>
                <li>Fail2ban para protección contra ataques</li>
            </ul>
        </div>

        <div class="info">
            <h3>🔗 Enlaces Útiles</h3>
            <p><a href="/health">Health Check</a> | <a href="https://nginx.org/en/docs/">Documentación Nginx</a></p>
        </div>

        <footer>
            <p>Desplegado el: <strong>{{ ansible_date_time.iso8601 }}</strong></p>
            <p>🚀 Infra Made Easy - Workshop de Ansible y AWS</p>
        </footer>
    </div>
</body>
</html>
